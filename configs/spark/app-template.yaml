apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: "{SPARK_APP_NAME}"
  namespace: spark
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: "{SPARK_IMAGE}"
  imagePullPolicy: Always
  mainApplicationFile: "{MAIN_APPLICATION_FILE}"
  arguments: []
  sparkVersion: 3.5.1

  restartPolicy:
    type: Never

  deps:
    packages:
      - org.apache.iceberg:iceberg-spark-runtime-3.5_2.12:1.5.2
      - org.apache.iceberg:iceberg-aws-bundle:1.5.2
  hadoopConf:
    fs.s3a.impl: org.apache.hadoop.fs.s3a.S3AFileSystem
    fs.s3a.aws.credentials.provider: com.amazonaws.auth.WebIdentityTokenCredentialsProvider,com.amazonaws.auth.InstanceProfileCredentialsProvider
    fs.s3a.path.style.access: "false"
  sparkConf:
    spark.kubernetes.authenticate.driver.serviceAccountName: spark-driver-sa
    spark.kubernetes.executor.deleteOnTermination: "true"
    spark.sql.session.timeZone: UTC

    spark.jars.ivy: /tmp/.ivy2
    spark.driver.extraJavaOptions: -Divy.cache.dir=/tmp/.ivy2/cache -Divy.home=/tmp/.ivy2
    spark.executor.extraJavaOptions: -Divy.cache.dir=/tmp/.ivy2/cache -Divy.home=/tmp/.ivy2

    spark.sql.extensions: org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions

  driver:
    cores: "{DRIVER_CORES}"
    memory: "{DRIVER_MEMORY}"
    serviceAccount: "{DRIVER_SERVICE_ACCOUNT}"
    nodeSelector:
      node-type: spark
    tolerations:
      - key: dedicated
        value: spark
        effect: NoSchedule
    env:
      - name: KAFKA_BOOTSTRAP
        value: "logistics-kafka-kafka-bootstrap.kafka.svc.cluster.local:9092"
      - name: ICEBERG_BUCKET
        value: "{ICEBERG_BUCKET}"
      - name: CHECKPOINTS_BUCKET
        value: "{CHECKPOINTS_BUCKET}"
      - name: CLICKHOUSE_HOST
        value: "clickhouse.clickhouse.svc.cluster.local"
      - name: CLICKHOUSE_PORT
        value: "8123"
    labels:
      component: spark-driver

  executor:
    cores: {EXECUTOR_CORES}
    instances: {EXECUTOR_INSTANCES}
    memory: "{EXECUTOR_MEMORY}"
    nodeSelector:
      node-type: spark
    tolerations:
      - key: dedicated
        value: spark
        effect: NoSchedule
    labels:
      component: spark-executor